{% set gitlab = salt['grains.filter_by']({
  'default': {
    'url': 'http:/to.itab.server',
    'token': 'change-token',
    'identifier': 'default'
    'enabled': false

  },
}, merge=salt['pillar.get']('gitlab:lookup')) %}
